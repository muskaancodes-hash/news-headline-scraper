# news-headline-scraper
A Python web scraper that collects top news headlines and saves them to a text file.

we imported the Requests library, specified the website URL, sent a request to the website, and checked the response status code.

we added BeautifulSoup to read and parse the website's HTMl

we extracted the news headlines from the website using h2 tags.
