# news-headline-scraper
A Python web scraper that collects top news headlines and saves them to a text file.

  In this first version, we imported the Requests library, specified the website URL, sent a request to the website, and checked the response status code.
